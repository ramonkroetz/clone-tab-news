{
  "name": "clone-tab-news",
  "version": "1.0.0",
  "description": "## NVM Version (.nvmrc) nvm install",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "npm run services:up && npm run wait-for-postgres && npm run migration:up && next dev",
    "lint:check": "prettier --check .",
    "lint:fix": "prettier --write .",
    "migration:create": "node-pg-migrate -m infra/migrations -j sql create",
    "migration:up": "node-pg-migrate -m infra/migrations --envPath .env.development --language ts up",
    "services:down": "docker compose -f infra/compose.yaml down",
    "services:stop": "docker compose -f infra/compose.yaml stop",
    "services:up": "docker compose -f infra/compose.yaml up -d",
    "test": "npm run services:up && npm run wait-for-postgres && concurrently -n next,jest --hide next -k -s command-jest 'next dev' 'jest --runInBand'",
    "test:watch": "jest --watchAll --runInBand",
    "typecheck": "tsc",
    "wait-for-postgres": "node --no-warnings=ExperimentalWarning --loader ts-node/esm infra/scripts/wait-for-postgres.ts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/async-retry": "^1.4.9",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "@types/react": "^19.0.1",
    "@types/react-dom": "^19.0.1",
    "chalk": "^5.3.0",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "prettier": "^3.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "async-retry": "^1.3.3",
    "dotenv": "^16.4.4",
    "dotenv-expand": "^11.0.6",
    "next": "^13.1.6",
    "node-pg-migrate": "^6.2.2",
    "pg": "^8.11.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
